// Smoke test placeholder. Neon Drift Board boots through the gray-flow
// DriftBootScreen, which needs an initialised GameState + native plugins —
// out of scope for a widget test. Real integration coverage lives in the
// game's own test suite.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
